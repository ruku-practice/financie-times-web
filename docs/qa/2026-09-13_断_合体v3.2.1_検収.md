# 断（デバッガー）検収メモ｜合体 v3.2.1（2026-09-13）

- 対象＝枝 `feature/analysis-tab` HEAD `eab130f1`（コード全体：css/・js/・tests/・index.html・scripts/）
- 深さ＝**標準**（v3.2.0 の断の申し送り＝検査の自己実行と故意に壊す確認）・依頼 11:52 JST・結果受領 12:08 JST（担当が 12:06 に切り上げを依頼）
- 転記＝合体の担当2代目（Opus 5）。原文は断の最終報告

## 判定：条件つき（重1が1件＝custom 期間の aria-pressed の残留）

## 自分で回した検査（断）
check_merged 29・check_merged_ref 31・check_merged_core 15・check_overview 80・check_overview_data 13・check_daily_sort 8・check_analysis_parts 6・check_analysis 57＝合計239件 ALL PASS（担当の実測と一致）

## 故意に壊した確認（3件とも正しく赤くなり、元に戻した・`git status`／`git diff --stat` で追跡ファイルの差分ゼロを確認）

| 壊した箇所 | 結果 |
|---|---|
| css/layout.css の重1の節（`.ov-header-tools{justify-content:flex-start}`＋`.ft-view-group{width:100%}`）を削除 | M17 が FAIL（「上位＋その他」が x=-94 で画面外＝不具合を再現） |
| js/merged-analysis.js の `register("conclusion", …, renderMembersKpi)` から renderMembersKpi を外す | M10・M11 が FAIL（期間を変えても KPI が変わらない＝直す前の不具合を再現） |
| css/advanced.css の `--series-9/10` を旧の青系 `hsl(214,45%,70.0%/68.4%)` に戻す | M14 が FAIL（9位・10位が ΔE 2.1） |

## 指摘

| 重さ | 場所 | 現象 | 担当の扱い |
|---|---|---|---|
| **重1** | js/overview.js の `pullMergedState` と `restoreStateFromUrl` の custom 分岐（計2か所） | 期間の釦を押したあと開始〜終了（custom）に切り替えると、`classList.remove("active")` だけで `aria-pressed` は触らない＝直前の釦（例 90日）が見た目は非活性なのに `aria-pressed="true"` のまま。ほかの組は setActive で同時に更新している。コードを読んでの検出（Playwright での再現は切り上げで未確認）。担当も 12:00 に同じ点を自己発見していた | v3.2.2 で直す（同じ形の3か所すべてに aria-pressed="false"）・検査 M24 を追加 |

## 見た点（依頼の観点）
1. 契約 C（期間のメンバー純増）：renderMembersKpi は `M().load("members")` の Promise の中でも `ctx.isCurrent()` を見る＝古い世代の上書きなし。computeRange／prevRange／membersNet は reference_merged.py と一致（check_merged_ref R3・check_merged M10）。窓の値（機運④）とは別経路で混線なし。
2. overview.js の renderRangeNote／updateControlsSummary／setupControlsToggle：無限ループ・二重描画なし（`dataset.bound` で二重バインド防止・recomputeAll 内で1回）。aria-pressed の custom 分岐だけ抜け（重1）。
3. CSS（layout.css 末尾）：≤768px で枠内（M17・故意破壊で確認）。PC は `.ov-controls-toggle{display:none}`（M16）。`.ov-ranking-head` への副作用なし（grep）。
4. 色：`--series-9/10` と OV_FALLBACK_COLORS が一致（M14・故意破壊で確認）。
5. 検査の強さ：M10〜M21・A21 の変更に緩めた形（`|| true`・空配列で PASS 等）なし。M17 の釦数27は内訳を確認し妥当。
6. 秘密の混入：今回の差分ファイルを再スキャン＝BLOCK 0・WARN 1（scripts/build_analysis_staging.sh:10 の `/Users/kkr`＝今回の差分で変わった行ではない・増加なし）。

## 未確認（断が明記）
- スマホ実機でのタップ（Playwright のタッチのエミュレーションのみ）
- 重1 の自動テストでの再現（Playwright の接続がタイムアウトし切り上げ）＝担当が M24 で確認する
