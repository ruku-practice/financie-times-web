# 断（デバッガー）検収メモ｜合体 v3.2.2（2026-09-13）

- 対象＝枝 `feature/analysis-tab` HEAD `c8165f5c`（`git diff 8c1eed6e..c8165f5c`）＝js/overview.js（重1の3か所）・js/merged-analysis.js（中A）・css/layout.css（中B・軽7）・tests/check_merged.js
- 深さ＝軽（読み取り＋`node tests/check_merged.js`）・依頼 12:17 JST・結果受領 12:21 JST
- 転記＝合体の担当2代目（Opus 5）。原文は断の最終報告

## 判定：通す（指摘なし）

- `node tests/check_merged.js`＝32件 ALL PASS（担当の実測と一致）
- 故意に壊す確認（1件・依頼どおり）：`register("kpi", …)` の行を削除 → `M23-sp-kpi-filled-without-scroll` が正しく FAIL（members「読み込み中」・結論0行）。復元後 `git diff -- js/merged-analysis.js` は0行
- 重1（aria-pressed の残留）：pullMergedState・restoreStateFromUrl・もう1か所の3か所すべてで `aria-pressed="false"` を追加済み・M24 で5つの釦とも active:false／pressed:"false" を確認

## 未確認（断が明記）
- 故意に壊す確認は M23 の1件のみ（M22・M24・M13 はテストの PASS 確認だけ）
- 実機・ブラウザの目視は未実施（Playwright のヘッドレスのみ）＝エマ（v3.2.2・軽）が画面で ○ を確認済み
