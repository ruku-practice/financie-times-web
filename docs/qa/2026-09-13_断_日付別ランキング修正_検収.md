# 断（デバッガー）検収メモ｜日付別ランキングの並べ替え修正 v3.1.1（2026-09-13）

- 対象＝枝 `fix/daily-sort`（起点 `cad3618d`＝本番 v3.1.0・コミット `416b5cc0`）と `scripts/release_fix_daily_sort.sh`（未追跡・元フォルダ）
- 深さ＝軽（依頼 09:24 JST・結果受領 09:30 JST）・読み取りのみ（試験は担当が回した結果を渡した）
- 転記＝担当（合体の担当・Opus 5）。原文は断の最終報告

## 判定：条件つき（本体の直しは通す。リリーススクリプトの2点は実行前に直す）→ 担当が2点とも対応済み

### 通った点（断の実測）
- 修正前ログ `tests/out/check_daily_sort_before_fix.log` で D1〜D4 が実際に FAIL。D2 の先頭 `[160962, 383602.6, 1315.2, 8.9, 179825.2]` がルクのスクショと一致＝壊れた状態で検査が本当に落ちる。
- 修正後 ALL PASS。D5 は992日全件を実際の釦のクリックで通し、1行しかない日（20231221）や出来高の同値（先頭50日中16日）も踏んでいる。
- D5 は `dailyData` を差し替えてから釦を押して `renderDailyTable` を通す＝`loadDailyData` と同じ描画関数の経路。日付変更側の穴を見逃す作りではない。
- `.sort-tab-btn` を使う他の箇所（overview.js・chart-legend.js）は ID 付きの容器に絞って拾っている、またはクラスが1グループにしか無い＝同じ型の穴は他に無い。
- 合体の枝（`feature/analysis-tab`）の `an-period-group`・`an-window-group` も `.sort-tab-btn` だが `.sort-criteria-group` の外。index.html の `.sort-criteria-group` は1個だけ＝合体後もこの直しで守られる。
- 秘密の混入（secret_scan.py）＝BLOCK 0件（WARN 2件はパスとメールの直書き＝既存スクリプトと同じ用途）。

### 指摘と担当の対応

| 重大度 | 場所 | 現象 | 担当の対応（09:31） |
|---|---|---|---|
| 中 | `release_fix_daily_sort.sh` ④⑤ | ⑤の VERIFIED.md 雛形が「`js/overview.js` SHA256 一致」と印字（実際に比べているのは advanced.js）。④は advanced.js しか比べず、版を上げた overview.js・chart-legend.js の本番反映を機械で確かめていない | ④を `js/advanced.js js/overview.js js/chart-legend.js` の3本の SHA256 一致に広げ、雛形の文言も「3本とも一致」に。ドライランで印字を再確認 |
| 軽 | 同 11行目のコメント | 「worktree（feature/overview-3.0.0）」と写し元の枝名が残っていた | `fix/daily-sort` に直した |
