# 断（デバッガー）検収メモ｜合体 v3.2.3 本番準備（P1 旧分析タブの撤去・P2 毎晩の集計）（2026-09-13）

- 対象＝枝 `feature/analysis-tab` HEAD `0b1c40b1`（`git diff 99973cda..0b1c40b1`・未push）
- 深さ＝軽（読み取り＋検査の自己実行）・依頼 12:44 JST・結果受領 12:58 JST
- 転記＝合体の担当2代目（Opus 5）。原文は断の最終報告

## 判定：通す（指摘なし）

## 自分で回した検査（断）
check_merged 33（M25 含む）・check_overview 80・check_merged_ref 31・check_merged_core 15・check_overview_data 13・check_daily_sort 8＝ALL PASS（担当の実測と一致）。8991 などのサーバーは停止を確認。

## 観点ごとの確認
| # | 観点 | 結果 |
|---|---|---|
| ① | workflow の bash（GitHub の既定 -eo pipefail）で if・checkout・commit の流れ | `build_overview.py && check_overview_data.py` は if 条件の中＝errexit で途中終了しない。`git push` は if/else の外で、作り直しが失敗しても必ず実行＝日次データの push が消える経路なし |
| ② | FT_CI の分岐が「常に成功」になっていないか | 3通りを自分で実行＝FT_CI=1＋正本なし→終了コード0（13/13）・FT_CI なし＋正本なし→1（merges_match_window_compare が NG）・通常→0。CI の分岐も `len(build_merges)==3` を残す＝無条件 PASS ではない |
| ③④ | ?tab=analysis の URL 書き換え・旧タブの参照の残り | M25 で `?tab=analysis&ov_p=30` から tab だけ消え ov_p=30 は残る・30日が選択中・data/analysis への通信0件（自分の実行でも再現）。grep でも data/analysis への fetch は残っていない。js/advanced.js の `analysisView` 変数と `analysis-tab` の分岐は null ガードつきの無害な残り |

## 未確認（断が明記）
- 故意に壊す確認はしていない（前の2回で同じ手法を実測済み・今回は独立の3通りの再現を優先）
- 実際の GitHub Actions ランナーでの daily.yml／metadata.yml の実行（rebase の衝突・並走のタイミング）＝本番の翌朝に観察

## 担当の追加の模擬（12:58）
使い捨ての複製で daily.yml の該当部分だけを取り出し `bash -eo pipefail` で実行（`python` は python3 を呼ぶ入口・git push は echo）：
- 成功の経路（FT_CI=1・正本なし）＝13/13・「chore: overview data (schema 2)」がコミットされ PUSH まで進む・終了コード0・market の最終日 2026-09-13
- 失敗の経路（検査が落ちる）＝12/13・`::warning::` を出して data/overview を HEAD に戻し、コミットを増やさず PUSH まで進む・終了コード0
