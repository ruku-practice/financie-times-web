# 断（デバッガー）検収メモ｜全体市況と「分析」の合体 v3.2.0（2026-09-13）

- 対象＝枝 `feature/analysis-tab` HEAD `b73daf84`・ステージング Version `be6ad4d2`
- 深さ＝軽（依頼 10:41 JST・結果受領 10:52 JST）・読み取りのみ（試験は担当の結果を根拠にした）
- 転記＝合体の担当（Opus 5）。原文は断の最終報告

## 判定：通す

### 確かめたこと（断）
- 秘密の混入：今回の差分（407dcd93..HEAD の js/css/index.html/scripts/tests・24ファイル）だけを再スキャン＝BLOCK 0件・WARN 1件（`scripts/build_analysis_staging.sh` の既定引数のローカルパス＝外に出ない）。リポジトリ全体の BLOCK 12件は既存ファイル（spec.md・test_html_sheets.py・scripts/update_metadata.py 等・2026-06 由来）で今回の差分ではない。
- 無限ループ・二重描画：`set(..., { silent: true })` は購読者へ知らせない。入れ替えた期間の書き戻しはこの経路で、`subscribe → scheduleRecompute` へ戻り込まない。`scheduleRecompute` は `recomputeQueued` でまとめている。
- 月次の先読み：`monthly.json` の採用日は PJ ごとに記録（Astra の指摘「月共通の picked_days」は解消）。
- 検査の定義の変更（A7・A8・A32・A36・A46）：既定の変更に合わせた入れ替えで、緩めた形（`|| true`・空配列で PASS 等）は無い。A7 はむしろ条件が1つ増えた。
- 毎晩の処理：daily.yml・metadata.yml に build_overview.py の呼び出しは無い＝設計書に既知の課題として明記済み・ステージング限定のため今回は問題にしない（本番反映は別の決裁）。

### 申し送り

| 重さ | 内容 | 担当の扱い |
|---|---|---|
| 中 | 深さを「軽」にしたが、差分の大きさ（9コミット・2,200行超・集計の契約）は _RULE.md の「標準」に当たる。契約 C の式の整合・隠れた見せ方の stale・箱の再描画は、読み取りのみでは実質検証できない。次に断を呼ぶときは「標準」にして、検査の自己実行（check_merged_ref.js 等）と故意に壊す確認を入れる | 次のセッションで「標準」の断を1回入れる（本番の決裁の前） |
| 軽 | リポジトリ全体に旧 Mac のユーザー名の直書き（BLOCK 12件）が既存 | 合体とは別の案件として社長室へ申し送る |

### 未検証（断が明記）
- 契約 C の式の実データ横断一致は、断が自分では試験を回していない（担当の check_merged_ref.js 31項目を根拠にした）。
- 見せ方の箱の stale・render が false を返す経路のブラウザでの動作確認は不実施。
