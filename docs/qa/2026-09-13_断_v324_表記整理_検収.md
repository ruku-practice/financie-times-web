# FiNANCiE TIMES Web v3.2.4（日付別ランキングの表記整理＋価格ランキング2種）断（軽）検収

判定：**通す**（壊そうとしたが、壊れなかった）
時刻：2026-09-13 13:59 JST（`TZ=Asia/Tokyo date "+%F %H:%M"` 実測）
深さ：**軽**　担当範囲：**コード**（js/advanced.js・index.html・css/layout.css・tests/・scripts/release_rank_notation.sh／scripts/build_rank_staging.sh）

対象：worktree `rank-notation`（枝 feature/rank-notation-3.2.4）コミット `1f7863ad`、`git diff 5657614f..1f7863ad`（13ファイル・+556/-102）

## 指摘の表

| # | 重さ | 場所 | 再現手順 | 期待と実際 |
|---|---|---|---|---|
| （なし） | ─ | ─ | ─ | 突き返す指摘なし（重0・中0・軽0） |

## 実行した検査と結果

1. **秘密の混入チェック**（`secret_scan.py`）
   - 今回の変更ファイル9本（`css/layout.css index.html js/advanced.js js/analysis.js js/overview.js scripts/build_rank_staging.sh scripts/release_rank_notation.sh tests/check_daily_sort.js tests/check_merged.js`）を対象に実行
   - **検出：BLOCK 0件／WARN 3件**（`scripts/build_rank_staging.sh:10`・`scripts/release_rank_notation.sh:29,33` の現ユーザー名／メールアドレス直書き）
   - WARNは通した理由＝この機体専用の手元デプロイ・リリーススクリプトで、配布物ではない。既存の兄弟スクリプト `scripts/release_merged.sh` にも同型の記述があり新規混入ではない
   - 参考：worktree全体を対象にすると `spec.md`・`scripts/*.py` に旧Macユーザー名（`/Users/kurokzhr`）のBLOCK12件があるが、これらは2026-06-26以来存在する既存ファイルで**今回の差分に含まれない**（`git diff 5657614f..1f7863ad --stat` に出てこない・`git blame` で当該行が98e8d2b7＝2026-06-26と確認）。担当範囲外の既存負債であり、本検収の対象外

2. **差分の境界の読み込み**（js/advanced.js の127行差分を全行精読）
   - `formatDiffText`：`diff===0`→`±0`、`typeof!=="number"||!isFinite`→`-`（`null`・`undefined`・文字列・`NaN`を先に弾く）。`-0`は`diff===0`の判定を先に通るためmark計算に届かず安全
   - `roundForDisplay`：表示の桁で丸めた**あとの値**が0なら「+0.01%未満」「▲1未満」に分岐。符号（mark）は丸める**前**の`diff`の正負で決めるため、`+0.004%`のような境界でも符号と「未満」表記が矛盾しない
   - `priceRateOf`：`base = price - price_diff`が0以下・非数なら`null`（0除算を回避）。`isPriceRankable`は`volume_24h >= 1000`（1,000円ちょうどは足切りを通る＝仕様どおり「以上」）かつ`priceRateOf!==null`
   - 並べ替え釦：`DAILY_SORTS`のキーだけを`hasOwnProperty`で許可（不正な`data-sort`値は`volume`にフォールバック）。釦の取得は`.sort-criteria-group .sort-tab-btn[data-sort]`とクラス＋親要素の二重指定＝v3.1.1で起きた「全体市況の釦と混線する」再発は見当たらず
   - データ取得失敗時：`dailyData = []`にしたうえで注記を隠し`btnLoadMoreDaily`を隠す＝直後に並べ替えを押しても前の日の行は残らない設計（D10で裏取り）

3. **`node tests/check_daily_sort.js` の自己実行**（サンドボックス外・Playwright使用・約5分）
   - 現状（正しいコード）で実行：**17/17 PASS**（`tests/out/check_daily_sort.json` `results`件数=17・全`ok:true`）
   - **故意に壊した1件**：`js/advanced.js` の `const mark = diff > 0 ? "+" : "▲";` を一時的に `const mark = "+";`（常にプラス表記）へ書き換えて再実行
     → **D5g-all-cells-sign-matches-data が FAIL**（1/17失敗・`bad=2901`件・例：`20231225 A_E_B col4 raw=-114816 text=+115`）。他16項目はPASSのまま＝この検査は「常に成功する形」になっておらず、符号の食い違いを実際に検出することを確認
   - 壊した行を元に戻し、`git status --porcelain --untracked-files=no` → `js/advanced.js` の差分は0行（`git diff -- js/advanced.js | wc -l` = 0）で復元を確認
   - **注意**：同じ`git status`に `M scripts/build_rank_staging.sh` と `share.html` 等の未追跡ファイルが残っているが、これは**別セッション（share.html担当）が同じworktreeで進行中の作業**であり、この断は触っていない・読んでいない（依頼の対象外指定どおり）

4. **`tests/out/check_merged.json` の既存結果を確認（自己実行はしていない）**
   - `at: 2026-09-13T04:44:21Z`＝JST 13:44（現HEADでの担当の実行・自分の検査時刻13:59の15分前・古い版ではないことを確認）
   - `allOk: true`・`results`件数=33・失敗0件

5. **`bash scripts/release_rank_notation.sh`（ドライラン既定・`--execute`は付けていない）**
   - 実行結果：**未コミット変更（`M scripts/build_rank_staging.sh`）を検知して安全に停止**（`git fetch`にも到達せず、何も変更していない）
   - この未コミット変更は上記3と同じ理由＝share.html担当の並行作業によるもので、この断が作ったものではない
   - **P1〜P4の前提を静的に個別確認**（ドライランがdirty検知で早期停止したため、フルパスは通らなかった代わりに各grep条件を単独実行）：
     - P1：`data-tab="analysis-tab"` 0件・`data/analysis` の追跡ファイル0件→OK
     - P2：`daily.yml`・`metadata.yml` とも `build_overview.py`・`check_overview_data.py` を含む→OK
     - P3：`index.html` の `?v=` は `3.2.4` のみ→OK
     - P4：並べ替え釦 `data-sort="..."` 4個・`js/advanced.js` に `const DAILY_SORTS` あり→OK
   - **実機確認は未了**：worktreeがクリーンな状態でのドライラン全体の流れ（①origin/main取り込み〜⑤雛形印字）・および`--execute`本番反映は、共存セッションのdirty状態のため今回は通せていない。次にクリーンな時点で再確認が要る

## 総括

壊そうといたした。……壊れませなんだ。

境界値（0・NaN・null・0除算・足切りの境界1,000円）・符号と丸めの食い違い・並べ替え釦の混線再発・データ無し日のあとの並べ替えを個別に当たったが、いずれも実装側で処理されており破綻を再現できず。検査（`check_daily_sort.js` D5g）を故意に壊して赤くなることも確認済み＝この検査は機能している。

ドライランスクリプトは、この断が作ったのではない並行作業の未コミット差分により「進む」経路までは検証しきれなかった（実機確認は未了と明記）。ただし**安全側に停止する**という肝心の挙動（クリーンでなければ何もしない）自体は実測で確認できた。

---
書き出し：/Users/kkr/ruku_data/00_products/apps/FiNANCiE-times-web/.claude/worktrees/rank-notation/docs/qa/2026-09-13_断_v324_表記整理_検収.md
