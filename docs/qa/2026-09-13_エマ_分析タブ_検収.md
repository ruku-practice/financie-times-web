# エマ（テスター・2回目の検収）｜「分析」タブ v0.1.0（2026-09-13）

| 回 | 時刻 | 結論 | 指摘 → 対応 |
|---|---|---|---|
| 1 | 07:04〜07:08 | 突き返し | 重1 スマホの表に列名が無い → 全セルに `data-label`／重2 判断資料のスクショが1画面だけ → 内側スクロール容器を開いて全区画（6,246px）／中3 モックの文言 → 実画面に合わせて描き直し／中4 横並び → 縦1枚ずつ／中5 導線 → Obsidian＋Antigravity／中6 「束」 → 区画4の注記／中7 「PJ」 → 冒頭に（PJ＝プロジェクト）／中8 自動文1行目の重複 → 集中度だけ／軽 判定の折り返し → nowrap |
| 2 | 07:13〜07:20 | 突き返し | 重1 スマホで指標#1の判定が枠外 → 数値セルを折り返す・tr の grid を minmax(0,1fr)／重2 「当てはまった指標＝3」 → 指標名で／中1 窓のラベル → 「窓（期末からさかのぼる日数・上の期間とは別）」／中2 期間＝窓のとき重複 → 出さない／中3 判断資料の検収表 → 書き直し／軽1 モックのタブ6つ／軽2 Antigravity 併記／軽3 凡例に（破線）（点線）／軽4 「本家↗」 |
| 3 | 07:23〜07:25 | **通す** | 残り＝判断資料の「再検収中」を差し替え／スマホの定義セル `td.def{min-width:220px}` を @media で 0 に → 07:27 対応・実測（375px）で tr の内側に収まることを確認 |

判定＝通す（07:25）／対象＝FiNANCiE TIMES Web 分析タブ v0.1.0（ステージング Version 92aca57c→再配信）／深さ＝重／担当範囲＝画面・文言・判断資料。
