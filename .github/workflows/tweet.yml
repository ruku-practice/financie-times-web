name: Daily FiNANCiE tweet update

on:
  repository_dispatch:
    types: [run-daily-tweet]
  # === B案（恒久対処 2026-07-12）: twikit 2.3.3 の KEY_BYTE バグで投稿が毎日全滅するため、
  # GitHub Actions での自動スケジュール投稿を停止。X投稿はローカル定期実行へ移設予定。
  # 詳細: company/output/2026-07/2026-07-12_CI恒久対処_調査レポート.md
  # 復活させたい場合は下の schedule ブロックのコメントを外す（invest リポと同じ dispatch/手動運用に統一）。
  # schedule:
  #   # フォールバック専用: 朝 5:10 JST = 20:10 UTC (通常の目標は 4:10 JST)
  #   - cron: "10 20 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-tweet
  cancel-in-progress: false

jobs:
  tweet:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Write service account credentials
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        run: |
          printf '%s' "$GOOGLE_CREDENTIALS_JSON" > service_account.json
          echo "wrote service_account.json"

      - name: Tweet FiNANCiE Report
        env:
          GOOGLE_CREDENTIALS_PATH: ${{ github.workspace }}/service_account.json
          X_COOKIES_JSON: ${{ secrets.X_COOKIES_JSON }}
        run: python scripts/py2tweet_free.py --sheet 08_FiNANCiE

      - name: Clean credentials
        run: rm -f service_account.json
