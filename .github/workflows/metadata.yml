name: FiNANCiE metadata update

on:
  repository_dispatch:
    types: [run-metadata-update]
  schedule:
    # 新規Slugの検出と画像URL変更の検出。cron指定は 14:30 UTC = 23:30 JST。
    # これが唯一の実行経路（元はローカルcron→2026-07に移植。Cloud Run にも Scheduler にも対応物はない）。
    # 「フォールバック専用」と書かれていた時代の主（Cloud Run）はもう存在しない。
    # 2026-07-27: 3時間前倒し（14:30 UTC → 11:30 UTC ＝ 23:30 JST → 20:30 JST・ルク決定）。
    # 旧設定では実測の起動が 00:32〜01:09 JST（62〜99分遅れ）となり、daily（0:13 JST起動）と
    # ほぼ同時に走っていた。「前夜にSlugを確認して翌0時の本処理に備える」という設計意図と
    # 順序が逆転し、新規Slugの取り込みが1日遅れていた。
    # 前倒し後は実測の76分遅延を見込んでも 21:30〜22:00 JST に走り、翌0:13 の daily より確実に先になる。
    # 2026-08-20: 🔴ガードの判定素材を daily の鮮度マーカー（projects_summary.json の latest_date）から
    # metadata 専用マーカー（data/metadata_last_run.txt）へ変更。旧判定は「朝に daily が押した当日印」を
    # 夜に見て常に『本日更新済み』と誤判定し、7/28〜8/19 の23日間、新規Slug検出が毎日スキップされていた
    # （success 表示のまま実質停止）。マーカーは本処理完走時のみ更新するので、同日二重起動だけを防ぐ。
    - cron: "30 11 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "本日分が既にあっても強制実行する"
        required: false
        default: false
        type: boolean
      test_mode:
        description: "Run without writing to Google Sheets"
        required: false
        default: false
        type: boolean
      pages:
        description: "Number of FiNANCiE list pages to scan"
        required: false
        default: "15"
        type: string
      skip_image_check:
        description: "Skip existing image URL checks"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: financie-metadata-update
  cancel-in-progress: false

jobs:
  metadata:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if already updated today (JST)
        id: check
        env:
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.force == true }}
        run: |
          TODAY=$(TZ=Asia/Tokyo date +%Y%m%d)
          LAST=$(cat data/metadata_last_run.txt 2>/dev/null || echo "")
          echo "today(JST)=$TODAY / metadata_last_run=$LAST"
          if [ "$FORCE_RUN" = "true" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "▶ force=true のため強制実行します。"
          elif [ "$LAST" = "$TODAY" ]; then
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "✅ 本日($TODAY)は既に更新済み。以降のステップをスキップします。"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "▶ 本日($TODAY)は未更新。実行します。"
          fi

      - name: Set up Python
        if: steps.check.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.check.outputs.run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright Chromium
        if: steps.check.outputs.run == 'true'
        run: python -m playwright install --with-deps chromium

      - name: Write service account credentials
        if: steps.check.outputs.run == 'true'
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        run: |
          printf '%s' "$GOOGLE_CREDENTIALS_JSON" > service_account.json
          echo "wrote service_account.json ($(wc -c < service_account.json) bytes)"

      - name: Run metadata update
        if: steps.check.outputs.run == 'true'
        env:
          GOOGLE_CREDENTIALS_PATH: ${{ github.workspace }}/service_account.json
          FINANCIE_EMAIL: ${{ secrets.FINANCIE_EMAIL }}
          FINANCIE_PASSWORD: ${{ secrets.FINANCIE_PASSWORD }}
          PYTHONUNBUFFERED: "1"
          TEST_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.test_mode == true }}
          PAGES: ${{ github.event_name == 'workflow_dispatch' && inputs.pages || '15' }}
          SKIP_IMAGE_CHECK: ${{ github.event_name == 'workflow_dispatch' && inputs.skip_image_check == true }}
        run: |
          ARGS=""
          if [ "$TEST_MODE" = "true" ]; then
            ARGS="--test"
          fi
          if [ -n "$PAGES" ]; then
            ARGS="$ARGS --pages $PAGES"
          fi
          if [ "$SKIP_IMAGE_CHECK" = "true" ]; then
            ARGS="$ARGS --skip-image-check"
          fi
          python scripts/update_metadata.py $ARGS

      - name: Record run marker
        if: ${{ steps.check.outputs.run == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.test_mode == true) }}
        run: TZ=Asia/Tokyo date +%Y%m%d > data/metadata_last_run.txt

      # 成功したので、過去に出した metadata のエラー告知を消す（期限切れ告知の掃除も兼ねる）
      - name: Clear site error notice
        if: ${{ steps.check.outputs.run == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.test_mode == true) }}
        run: python3 scripts/site_notice.py clear-error --source metadata

      - name: Commit and push changes
        if: ${{ steps.check.outputs.run == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.test_mode == true) }}
        run: |
          rm -f service_account.json
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No metadata changes."
          else
            git commit -m "chore: update FiNANCiE metadata ($(TZ=Asia/Tokyo date +%Y-%m-%dT%H:%MJST))"
            git pull --rebase --autostash origin main
            git push
          fi

      # 失敗時：日付別ランキング上部にエラー告知を出す（次回成功時に自動で消える）
      - name: Report failure to site
        if: ${{ failure() && steps.check.outputs.run == 'true' }}
        run: |
          rm -f service_account.json
          python3 scripts/site_notice.py add-error --source metadata \
            --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/site_notices.json
          git diff --cached --quiet && exit 0
          git commit -m "chore: site error notice (metadata failure)"
          git pull --rebase --autostash origin main
          git push
